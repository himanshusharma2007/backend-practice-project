<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="bg-gray-900 text-gray-100 font-['Inter']">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
      <header class="flex justify-between items-center mb-12">
        <h1 class="text-3xl font-bold">Edit Post</h1>
        <a href="/logout">
          <button class="bg-red-600 hover:bg-red-700 px-6 py-2 rounded-full text-lg transition duration-300 ease-in-out">
            Log out
          </button>
        </a>
      </header>

      <form action="/update/<%= post._id%>" method="post" class="space-y-6">
        <div>
          <label for="content" class="block text-lg font-medium mb-2">Enter Updated Post</label>
          <textarea
            id="content"
            name="content"
            rows="6"
            class="w-full p-4 bg-gray-800 rounded-lg resize-none border border-gray-700 focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50 transition duration-300 ease-in-out"
          ><%= post.content%></textarea>
        </div>
        <button type="submit" class="bg-yellow-600 hover:bg-yellow-700 px-6 py-3 rounded-full text-lg font-semibold transition duration-300 ease-in-out">
          Update post
        </button>
      </form>
    </div>
  </body>
</html>